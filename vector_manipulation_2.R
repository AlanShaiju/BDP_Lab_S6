#Write a R program to compute the sum, mean and product of a given vector element.
myvector<-c(4,6,8,9,3,2,4,6,7,6,2,2,2)
cat('mean of my vector:',mean(myvector))
cat('sum of my vector:',sum(myvector))
cat('product of my vector:',prod(myvector))